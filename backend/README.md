# Backend

This is a simple Express.js backend server.

## Features
- Express server
- CORS enabled
- JSON middleware
- Sample API route: `/api/users`

## Scripts
- `npm start` — Start the backend server

## Getting Started
1. Run `npm install` to install dependencies
2. Run `npm start` to start the server
